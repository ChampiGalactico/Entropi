export function SettingsPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-text-primary">Settings</h1>
      <p className="mt-1 text-sm text-text-muted">Lookup tables, locations, grading, and theming config will live here.</p>
    </div>
  );
}
